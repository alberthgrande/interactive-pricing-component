const rangePrice = document.getElementById("range-price");
const views = document.getElementById("views");
const price = document.getElementById("price");

price.innerText = `$${100}.00`;
views.innerText = `${10}K`;
